@model BiuroPodrozy.Intranet.ModelsForView.IndexTableViewModel<BiuroPodrozy.Data.Language>

@{
    var index = (Model.CurrentPage - 1) * Model.ItemsPerPage;
    var dummy = Model.Items.FirstOrDefault();
    ViewData["Title"] = "Index";
}

<table class="table table-striped table-sm">
    <thead>
        <tr>
            <th>
                #
            </th>
            <th>
                @Html.DisplayNameFor(model => dummy.LanguageName)
            </th>
            <th>
                @Html.DisplayNameFor(model => dummy.AbbrLanguageName)
            </th>
            <th>

            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.PaginatedItems())
        {
            <tr>
                <td>
                    @(++index)
                </td>
                <td>
                    @Html.DisplayFor(modelitem => item.LanguageName)
                </td>
                <td>
                    @Html.DisplayFor(modelitem => item.AbbrLanguageName)
                </td>
                <td class="text-end">
                    <div class="btn-group btn-group-sm" role="group" aria-label="Button Group">
                        <a asp-action="Edit" class="btn btn-dark-blue" asp-route-id="@item.IdLanguage">
                            <i class="bi-pencil"></i>
                        </a>
                        <a asp-action="Details" class="btn btn-dark-blue" asp-route-id="@item.IdLanguage">
                            <i class="bi-search"></i>
                        </a>
                        <a asp-action="Delete" class="btn btn-dark-blue" asp-route-id="@item.IdLanguage">
                            <i class="bi-trash"></i>
                        </a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
@await Html.PartialAsync("_PaginationLayout", Model)