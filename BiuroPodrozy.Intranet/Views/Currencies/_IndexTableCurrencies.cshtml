@model BiuroPodrozy.Intranet.ModelsForView.IndexTableViewModel<BiuroPodrozy.Data.Currency>

@{
    var index = (Model.CurrentPage - 1) * Model.ItemsPerPage;
    var dummy = Model.Items.FirstOrDefault();
    ViewData["Title"] = "Index";
}
<table class="table table-striped table-sm">
    <thead>
        <tr>
            <th>
                #
            </th>
            <th>
                @Html.DisplayNameFor(model => dummy.CurrencyName)
            </th>
            <th>
                @Html.DisplayNameFor(model => dummy.CurrencySymbol)
            </th>
            <th>
                @Html.DisplayNameFor(model => dummy.AbbrCurrency)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.PaginatedItems())
        {
            <tr>
                <td>
                    @(++index)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CurrencyName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CurrencySymbol)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.AbbrCurrency)
                </td>
                <td class="text-end">
                    <div class="btn-group btn-group-sm" role="group" aria-label="Button Group">
                        <a asp-action="Edit" class="btn btn-dark-blue" asp-route-id="@item.IdCurrency">
                            <i class="bi-pencil"></i>
                        </a>
                        <a asp-action="Details" class="btn btn-dark-blue" asp-route-id="@item.IdCurrency">
                            <i class="bi-search"></i>
                        </a>
                        <a asp-action="Delete" class="btn btn-dark-blue" asp-route-id="@item.IdCurrency">
                            <i class="bi-trash"></i>
                        </a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
@await Html.PartialAsync("_PaginationLayout", Model)